# Public OAuth client ID of type "Web application". Never put a client secret here.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
# Existing same-origin session endpoint. Leave empty until the backend is ready.
# Contract: POST JSON { credential }, HttpOnly session cookie + HTTP 204 on success.
EXPO_PUBLIC_GOOGLE_SESSION_PATH=
